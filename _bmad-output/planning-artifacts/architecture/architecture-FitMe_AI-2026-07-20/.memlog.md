---
scope: FitMe AI MVP v1
purpose: Build substrate spine for downstream epics/stories + tech stack justification
altitude: initiative->features
updated: 2026-07-20T22:50
---

- (version) Verified 2026: Next.js 16 (App Router, Server Actions, Turbopack, cacheComponents/PPR) is settled baseline; Better Auth = recommended TS-first self-hosted auth (DB sessions, immediate revocation, email/password built-in)
- (constraint) Inherited from PRD: provider-agnostic AI layer; schema-validated AI outputs; per-user data isolation; no health data in logs; encryption in transit; PWA; offline instant-path
- (decision) Backend = Next.js 16 full-stack (server actions + route handlers) behind a strict server-only Data Access Layer. Prevents: scattered auth/ownership checks, two-codebase drift
- (decision) Auth = Better Auth (email/password, DB-backed sessions, immediate revocation, full data ownership). Prevents: third-party custody of sensitive health identity, JWT revocation gaps
- (decision) Hosting DEFERRED: keep deployment-agnostic (containerizable, env-driven config, no vendor lock-in in app code). Data residency (Singapore/near SL) to weigh at deploy
- (decision) AI v1 default = Google Gemini behind provider-agnostic interface with Zod-validated structured outputs. Provider swappable via adapter
- (decision) Distilled spine: 13 ADs (DAL choke point, validated server actions, DB-source-of-truth nutrition, AI port+schema-validation, AI guardrails, Better Auth DB sessions, user-scoped ownership, soft-delete+audit, no-PII-logs, UTC+profile-tz, canonical units, offline reconcile, uniform id/error). Layered App Router + ports-and-adapters paradigm
- (event) spine finalized
